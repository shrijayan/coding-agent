# Coding Agent

A tiny AI coding assistant you run in your terminal. You describe what you
want in plain English, and it actually reads your files, writes code, fixes
bugs, and runs commands to get it done - it doesn't just describe what to
do, it does it.

Think of it like hiring a junior developer who sits at your keyboard: you
give them a task in plain English, they open the right files, make the
change, run it to check it works, and tell you what they did.

This guide assumes no prior experience with any of these tools. Follow the
numbered steps in order and you'll have it running.

---

## Contents

1. [See it in action](#1-see-it-in-action)
2. [What it can and can't do](#2-what-it-can-and-cant-do)
3. [Before you start](#3-before-you-start)
4. [Setup, step by step](#4-setup-step-by-step)
5. [Running your first demo](#5-running-your-first-demo)
6. [Settings you can tweak](#6-settings-you-can-tweak)
7. [If something goes wrong](#7-if-something-goes-wrong)
8. [Important safety note](#8-important-safety-note)
9. [Want to see how it works inside?](#9-want-to-see-how-it-works-inside)

---

## 1. See it in action

Here's a real example of what a session looks like. `you>` is what a
person typed; everything else is the agent working and answering:

```
Coding Agent ready (anthropic / claude-sonnet-5). Type 'exit' to quit.

you> Create a file called hello.py that prints "Hello World", then run it
  [tool] write_file({'path': 'hello.py', 'content': 'print("Hello World")\n'})
  [tool] bash({'command': 'python3 hello.py'})

agent> Created `hello.py` with `print("Hello World")` and ran it — output: `Hello World`.

you> exit
Bye.
```

This is a real, unedited run of the exact prompt above (using the
`anthropic` provider - see [Setup](#4-setup-step-by-step) for the
OpenRouter alternative). The `[tool] ...`
lines are the agent showing its work - it's telling you exactly which file
it touched or which command it ran, so nothing happens behind your back.
The wording of the final `agent>` answer is generated by the AI, so it
will be phrased a little differently each time you run it - that's
expected, not a bug.

---

## 2. What it can and can't do

**It can, today:**
- Read a file
- Create a new file, or overwrite an existing one
- Edit one exact part of a file (a precise find-and-replace, not a guess)
- Run terminal/shell commands (e.g. run a script, run tests, install a package)
- List the files and folders in a project

**It cannot do (yet) - good to know before a demo:**
- It does **not** ask "are you sure?" before writing a file or running a
  command (see the [safety note](#8-important-safety-note) below)
- It does **not** remember past sessions - closing the terminal clears
  the conversation
- It talks to **one AI model at a time** - either Claude directly, or
  any model available through OpenRouter (GPT, Gemini, Llama, and
  hundreds more) - chosen in your settings, not switchable mid-conversation

---

## 3. Before you start

You'll need three things. None of them cost anything to set up except the
last one, which is pay-per-use (details below).

| # | What | Why |
|---|------|-----|
| 1 | A terminal | The text window you type commands into - "Terminal" on Mac, "PowerShell" or "Git Bash" on Windows, any terminal on Linux |
| 2 | `uv` (a Python project tool) | Installs the exact small set of libraries this project needs, in its own private folder, without touching anything else on your computer |
| 3 | An API key | The password-like code that lets this program talk to an AI model on your behalf. Step 4 below explains your two options: Anthropic directly, or OpenRouter (access to many providers with one key) |

You do **not** need to know Python to use this - you only need to type the
commands below exactly as written.

---

## 4. Setup, step by step

### Step 1 - Install `uv`

Open your terminal and run the command for your operating system:

**Mac or Linux:**
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

**Windows (PowerShell):**
```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```

Close your terminal window and open a new one (so it picks up the change),
then check it worked:

```bash
uv --version
```

You should see something like `uv 0.11.28`. If instead your terminal
says it doesn't recognize `uv`, see [troubleshooting](#7-if-something-goes-wrong).

### Step 2 - Get the project code

```bash
git clone https://github.com/shrijayan/coding-agent.git
cd coding-agent
```

> This repository is **private**. If you're setting this up for someone
> else's demo, that person needs to be added as a collaborator on GitHub
> first, or you can hand them the folder directly instead of cloning.

### Step 3 - Install the project's dependencies

```bash
uv sync
```

This downloads the small handful of libraries the code needs (to talk
to Claude and/or OpenRouter, and to read the settings file you'll
create in Step 5) into a
private folder called `.venv` inside the project. It does not install
anything globally on your computer.

### Step 4 - Get an API key

You have two options. Both work equally well - pick one:

| | Option A: Anthropic direct | Option B: OpenRouter |
|---|---|---|
| **Best for** | The simplest setup, using Claude specifically | Flexibility to try different models (GPT, Gemini, Llama, and hundreds more) without changing anything but a setting |
| **Get a key at** | [console.anthropic.com/settings/keys](https://console.anthropic.com/settings/keys) | [openrouter.ai/keys](https://openrouter.ai/keys) |
| **Key looks like** | `sk-ant-...` | `sk-or-...` |

Either way:
1. Sign up or log in at the link above.
2. Add a payment method (usually under **Billing** or **Credits**). This
   is separate from any claude.ai or ChatGPT subscription you may already
   have - these APIs are billed by how much you use them, not a flat
   monthly fee. Pricing is per "token" (roughly ¾ of a word): a few
   dollars per **million** tokens read and a bit more per million
   written. A demo session with a handful of requests typically costs a
   few cents, well under a dollar. See
   [Anthropic's pricing](https://docs.claude.com/en/docs/about-claude/pricing)
   or [OpenRouter's model list](https://openrouter.ai/models) (which
   shows per-model pricing) for current rates.
3. Create a key, give it any name, and copy it. You won't be able to see
   it again after you navigate away, so copy it now.
4. Treat this key like a password - don't share it or post it publicly.

### Step 5 - Configure the project

Copy the example settings file to create your own:

```bash
cp .env.example .env
```

Open the new `.env` file in any text editor (TextEdit, Notepad, VS Code)
and fill in two things, matching whichever option you picked in Step 4:

**If you got an Anthropic key (Option A):**
```
AGENT_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-your-key-goes-here
AGENT_MODEL=claude-sonnet-5
```

**If you got an OpenRouter key (Option B):**
```
AGENT_PROVIDER=openrouter
OPENROUTER_API_KEY=sk-or-your-key-goes-here
AGENT_MODEL=anthropic/claude-sonnet-5
```
(With OpenRouter you can swap `AGENT_MODEL` for any model slug from
[openrouter.ai/models](https://openrouter.ai/models), e.g.
`openai/gpt-5.1` or `google/gemini-3-pro-preview` - no other change
needed.)

Leave every other line as it is for now - the defaults are fine for a
demo. (What each one does is explained in
[Settings you can tweak](#6-settings-you-can-tweak).)

### Step 6 - Run it

```bash
uv run coding-agent
```

You should see something like:

```
Coding Agent ready (anthropic / claude-sonnet-5, optimizations: none). Type 'exit' to quit.

you>
```

(The `(anthropic / claude-sonnet-5)` part reflects whatever you set in
Step 5 - it'll say `openrouter / ...` if you went with Option B.)

You're in. Type a request and press Enter.

---

## 5. Running your first demo

Try these in order - each one shows off a different capability, and
they're safe to run in a throwaway folder:

1. **Explore a project**
   `List all the files in this project`

2. **Create something**
   `Create a file called notes.txt with a 3-line poem about the ocean`

3. **Read and explain**
   `Read notes.txt and tell me what's in it`

4. **Edit precisely**
   `Add one more line to notes.txt about the moon`

5. **Write and run code**
   `Write a Python script that prints the first 10 Fibonacci numbers, then run it and show me the output`

6. **Check what it cost**
   `/usage`
   - Prints tokens used, estimated cost, and how many messages/tool
     calls happened this session. This isn't a request to the AI at
     all - it's a built-in command the terminal itself understands
     (any input starting with `/`).

To leave, type `exit` (or press Ctrl+C).

**Reading the screen while it works:**
- `you>` - a line you typed
- `  [tool] name({...})` - the agent telling you exactly what it's doing
  right now (which file, which command) before it does it
- `agent>` - its final answer to you, after it's done acting
- `error>` - something went wrong reaching the model (see
  [troubleshooting](#7-if-something-goes-wrong)) - the session stays
  open, so you can just try again

**If you're running this as part of a workshop on cost/performance
optimizations:** start the agent with `uv run coding-agent --enable
some-optimization-name` (see what's available in `AGENTS.md`'s "How to
add a new optimization"). Run the same prompt with and without the
flag, and compare the two `/usage` outputs - that side-by-side is the
whole point.

For a more rigorous before/after than a hand-typed prompt, `uv run
coding-agent --benchmark` (optionally with `--enable`) runs a fixed
suite of real coding tasks and prints resolved-count/tokens/cost/time -
see `AGENTS.md`'s "Benchmark" section. This takes a few minutes (it's
setting up real, isolated environments per task), so it's better suited
to "run it, go grab a coffee, compare the two reports" than a live
step-by-step demo.

---

## 6. Settings you can tweak

All settings live in your `.env` file (created in Step 5).

| Setting | What it controls | Default | When you'd change it |
|---|---|---|---|
| `AGENT_PROVIDER` | Which AI provider to use: `anthropic` or `openrouter` | *(required, no default)* | Switch it (plus the matching API key and model below) to change provider entirely |
| `ANTHROPIC_API_KEY` | Your secret key to talk to Claude directly | *(required if `AGENT_PROVIDER=anthropic`)* | Only needed for that provider |
| `OPENROUTER_API_KEY` | Your secret key to talk to OpenRouter | *(required if `AGENT_PROVIDER=openrouter`)* | Only needed for that provider |
| `AGENT_MODEL` | Which model answers you | `claude-sonnet-5` | Change to any model your provider supports - see Step 4/5 above |
| `AGENT_MAX_TOKENS` | The longest answer the model can give in one go | `4096` | Raise it if long answers seem to get cut off |
| `AGENT_MAX_ITERATIONS` | Max number of read/write/run steps allowed for one request, so it can't loop forever | `25` | Raise it for a task that genuinely needs many steps |
| `AGENT_BASH_TIMEOUT_SECONDS` | How long one terminal command is allowed to run before it's force-stopped | `60` | Raise it if you ask it to run something slow (e.g. a big test suite) |

---

## 7. If something goes wrong

| You see | It means | Fix |
|---|---|---|
| `Configuration error: Missing required environment variable 'X'` | Your `.env` file is missing, or you skipped a line | Re-run `cp .env.example .env` and make sure every line has a value |
| `AGENT_PROVIDER must be one of: anthropic, openrouter` | You typed the provider name wrong in `.env` | Set it to exactly `anthropic` or `openrouter` (lowercase) |
| `command not found: uv` (or similar, wording differs by terminal) | `uv` isn't installed yet, or your terminal hasn't picked it up | Re-run the install command from Step 1, then fully close and reopen your terminal |
| `error> Claude API returned an error (401): invalid x-api-key` | Your Anthropic key is wrong, incomplete, or has no billing enabled | Fix `ANTHROPIC_API_KEY` in `.env`, confirm billing is set up at console.anthropic.com, then just try your request again - no need to restart |
| `error> OpenRouter returned an error (401): ...` | Your OpenRouter key is wrong or has no credits | Fix `OPENROUTER_API_KEY` in `.env`, confirm you have credits at openrouter.ai, then just try again |
| `error> ... returned an error (429): ...` | You've hit a rate limit (too many requests too quickly) | Wait a few seconds and try again |
| It pauses for a while, then errors about a timeout | A command took longer than `AGENT_BASH_TIMEOUT_SECONDS` | Raise that number in `.env` |
| `Stopped after N tool-use round-trips...` | The task needed more steps than `AGENT_MAX_ITERATIONS` allows | Raise that number in `.env`, or split your request into smaller asks |

Still stuck? Copy the exact error message - that's the fastest way for
anyone helping you to diagnose it.

Any line starting with `error>` means something went wrong talking to
the model (bad key, no internet, rate limit, the provider's servers
being temporarily down, ...) - it's shown as a clean one-line message
and the session stays open, so you can just fix the cause and try again
without restarting.

---

## 8. Important safety note

This agent **can create, overwrite, and delete real files, and run any
terminal command**, in whatever folder you launch it from - without
pausing to ask "are you sure?" first (that confirmation step doesn't
exist yet in this version).

Before a demo:
- Run it inside a project that's saved with `git`, or a throwaway test
  folder, so you can always undo changes (`git checkout .`) if needed.
- Don't point it at anything you can't afford to lose or break.
- Don't ask it to run a command you wouldn't be comfortable running yourself.

---

## 9. Want to see how it works inside?

This README is about *using* the agent. For how it's built internally
(the code architecture, the design decisions, how to add a new
capability), see [`AGENTS.md`](./AGENTS.md).
